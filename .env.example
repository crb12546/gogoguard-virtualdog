# 复制成 .env 后填。狗只需要这些；没有 DASHSCOPE_API_KEY 也能跑(图退回自带样例图)。

# 平台上报地址（默认 http://localhost:8000；指向生产则 https://gogoguard.cn）
BACKEND_URL=http://localhost:8000

# 百炼 key：仅用于 --material 时万相生成现场图/突发图。留空 → 用 assets/inspections 自带样例图。
DASHSCOPE_API_KEY=
WANX_MODEL=wanx2.1-t2i-turbo
WANX_VIDEO_MODEL=wanx2.1-t2v-turbo
